import React from "react";
import "animate.css/animate.min.css";

const EdParag = ({ children }) => {
  return (
    <li className='animated fadeIn'>
      {children}
    </li>
  );
};

export default EdParag;
